<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (user) {%>
    <h1>WELCOME BACK <%= user.username %></h1>

    <% if (user.member) {%>
    <h1>You are a member of this club</h1>
    <% } %> <% if (user.admin) {%>
    <h1>You are an admin of this club</h1>
    <% }} %>
    <p>Welcome to <%= title %></p>
    <% if (!user) {%>
    <p><a href="/log-in">Log In</a></p>
    <p><a href="/sign-up">Sign Up</a></p>
    <% } else if (user) {%>
    <p><a href="/log-out">Log Out</a></p>
    <% if (!user.member) {%>
    <p><a href="/join-member">Become a member</a></p>
    <% } if (!user.admin) {%>
    <p><a href="/become-admin">Become an admin</a></p>
    <% } if (user.member) {%>
    <p><a href="/write-message">Write message</a></p>
    <% }} for(let message of message_list) {%>
    <div>
      <div>
        <% if (!user) {%>
        <p>Become a member to see author and date</p>
        <% } else if (user.member) { %>
        <p><%= message.author.username %></p>
        <p><%= message.formatedDate %></p>
        <% } %>
      </div>
      <h3><%= message.title %></h3>
      <p><%= message.message %></p>
      <% if (user) {%> <% if (user.admin || message.author.username === user.username ) {%>
      <form action="/" method="POST">
        <input type="hidden" id="messageId" name="messageId" required="true" value="<%= message._id%>" />
        <input type="submit" value="Delete" />
      </form>
      <% }} %>
    </div>
    <% } %>
  </body>
</html>
